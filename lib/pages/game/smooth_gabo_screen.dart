import 'package:flutter/material.dart';

class SmoothGaboScreen extends StatelessWidget {
  const SmoothGaboScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
