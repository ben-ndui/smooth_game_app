import 'package:flutter/material.dart';

class SmoothPyramidScreen extends StatelessWidget {
  const SmoothPyramidScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
